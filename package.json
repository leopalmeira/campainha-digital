{
  "name": "campainha-digital-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd frontend && npm install --frozen-lockfile || npm install && npm run build",
    "start": "node backend/server.js"
  }
}
